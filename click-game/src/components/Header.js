import React from 'react';

const Header = () => {
    return(
        <div className="jumbotron">
            <h1>The Office Click Game!</h1>
            <h3>Click on a character to earn points, but don't click on the same one twice!</h3>
        </div>
        );
}

export default Header;